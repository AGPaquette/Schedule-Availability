export default function StaticButton(props) {
    return (
        <div className="container">
        <button className="form-control btn btn-primary">Click Me</button>
        </div>
    );
};